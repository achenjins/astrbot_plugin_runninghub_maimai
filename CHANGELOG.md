# 更新日志

## v2.7.0

- 支持在聊天里直接用自然语言发起生成：说需求就行，插件会挑选合适的工作流，也可以直接点名某个工作流
- 当前消息或引用消息里带的图片会自动用作参考图
- 多张图可以说「第一张当主体，第二张当风格」；没说清各张用途时会先问一句
- 缺少必填的图片或内容时会引导补齐，不会直接提交
- 可以在之前的任务上接着改，比如「上次那张把背景换成蓝色」，插件会沿用原任务的提示词、参数和参考图
- 可编辑参数只在你明确提及时修改，比如「宽度 1024，其他不变」
- 支持用自然语言管任务：「取消刚才那个」取消任务；「第二张没收到，再发一次」补发结果，不重新生成
- 结果发回后，机器人会沿用原会话的模型和人格再回一句，方便接着提要求
- 插件按用户和会话记住最近用过、生成的图片，默认 5 张（可设 0–20，设 0 关闭），同一内容自动去重，每张图带一句简短描述
- 支持用 QQ 头像当参考图，比如「画一下我的头像」，群里也能用群头像
- 每个工作流可单独开关「允许聊天调用」，关掉后自然语言不会选到它，命令仍可正常用

## v2.6.0

- `/wf 上传提示词` 改为上传可在 `/wf 提示词` 中选择运行的提示词文件
- 原模板上传功能改名为 `/wf 上传提示词模板`
- 多工作流配置下上传提示词会先选择对应工作流

## v2.5.1

- 修复 NapCat 群文件通知被前置输入收集器拦截，导致 `/wf 上传提示词` 无法接收文件
- 修复 OneBot 返回文件 URL/路径时被误判为 Base64，避免上传后出现乱码字节

## v2.5.0

- `/wf 提示词` 支持回复 `删除1` 删除已保存提示词，并在删除后刷新列表
- 新增 `/wf 上传提示词`，支持通过聊天上传 `.md` / `.txt` 扩写模板
- 上传模板自动识别常见中文编码并统一转为 UTF-8，减少中文乱码
- 精简提示词选择、文件收集和配置确认提示

## v2.4.2

- 按 AstrBot 指令组规范注册 `/wf 保存提示词`、`/wf 提示词重跑` 和 `/wf 提示词`，修复带空格命令无法触发的问题

## v2.4.1

- 修复 NapCat 发送远程视频时的富媒体上传失败：视频段失败后自动按文件发送同一视频

## v2.4.0

- 自动按用户缓存最近 5 条实际提交的扩写提示词，并保存扩写前描述与原工作流
- 新增 `/wf 保存提示词`，通过编号选择最近提示词并填写描述后持久保存
- 新增 `/wf 提示词重跑` 和 `/wf 提示词`，复用扩写结果并重新收集文件与可编辑参数，不重复调用 LLM

## v2.3.3

- README 计数徽章修正，体验工作流说明默认展开

## v2.3.2

- Pages 打开后自动查询国内外 RunningHub 余额，无需手动点「刷新余额」
- 新增插件更新日志文件（WebUI 插件详情 → 更新日志）

## v2.3.1

- 将两个 Pages 合并为一个页面：打开 `workflow-editor` 后，用顶部「工作流配置 / 任务记录」标签切换
- 删除独立的 `task-history` 页面，避免 AstrBot 卡片快捷入口默认打开任务记录页的问题
- 顶部「刷新」按钮按当前标签页刷新对应内容；配置页按钮在任务记录标签下自动隐藏

## v2.3.0

- 配置页顶部新增 RunningHub 余额面板：海外 / 国内分别显示 RH 币、运行中任务数、钱包余额和 API 类型
- 新增最近任务记录：只保留任务 ID、工作流名称、消耗 RH 币三个字段
- 成功任务完成后自动写入 `data/plugin_data/<插件目录>/task_history.json`，最多保留 200 条，页面支持刷新、复制任务 ID、清空
- 后端新增 `POST /uc/openapi/accountStatus` 账户查询，任务消耗取自 `/openapi/v2/query` 返回的 `usage.consumeCoins`
- 新增 `task_history.json` 读取、去重、上限裁剪与原子写入

## v2.2.6

- 精简 README 并补充 Pages 使用说明
- 更新插件描述与标签（生图 / 工作流 / 生视频 / AI）
